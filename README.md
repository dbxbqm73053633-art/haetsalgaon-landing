# 햇살가온 랜딩페이지

## Cloudflare Pages 환경변수

카카오맵을 표시하려면 Cloudflare Pages 프로젝트의 환경변수에 아래 값을 등록해야 합니다.

```env
VITE_KAKAO_MAP_KEY=카카오_JavaScript_키
```

로컬 개발에서는 `.env.example`을 참고해 `.env` 파일을 만들면 됩니다. 실제 `.env` 파일은 GitHub에 올리지 않습니다.
